<h1>All Article View</h1>
<% console.log(articles) %>
    <ul>
        <% articles.forEach((article)=> { %>
            <li>
                <%= article.title %>
            </li>
            <% }) %>
    </ul>

    </body>

    </html>